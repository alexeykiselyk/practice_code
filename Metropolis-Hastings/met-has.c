#include <stdio.h>
#include <math.h>
#include <stdlib.h>

/* Code to implement the Metropolis algorithm on a 2D array of particles */

int array_generator() {
    /* Create a 10*10 grid of numbers */
    srand(93045);
    double data[10][10];
    // for(int i = 0; i < 10; i++) {
    //     for(int j = 0; j < 10; j++) {
    //         if(rand() )
    //     }
        
    // }
}

int main() {
    srand(93045);
    printf("%ld", rand());
    return 0;
}